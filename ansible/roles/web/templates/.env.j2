# Next.js Production Environment Variables
NEXT_PUBLIC_API_URL=/api
NODE_ENV=production

# Optional: Add these if your frontend needs them
# NEXT_PUBLIC_APP_URL=https://<frontend_domain>
# NEXT_TELEMETRY_DISABLED=1
